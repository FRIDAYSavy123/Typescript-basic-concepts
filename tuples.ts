// let imrandata :  [number ,string]
// imrandata = [1, "s"]
// console.log(imrandata);

// let raffayData : (string | number) []
// raffayData = ["raffay" , 2 , ]

let [preferences, name, incountry] =
  Math.random() < 0.5 ? [1, "raffay", true] : [2, "wasay", false];
  console.log();
  